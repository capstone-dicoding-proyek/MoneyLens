services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: moneylens-api
    restart: unless-stopped
    env_file: .env.production
    ports:
      - "${PORT:-5000}:5000"
    networks:
      - moneylens-network

networks:
  moneylens-network:
    driver: bridge